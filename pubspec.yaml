name: gamified_space_screen
description: gamified_space_screen.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  animate_do: ^2.0.0
  cupertino_icons: ^1.0.2
  http: ^0.13.3
  shimmer: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/

  fonts:
    - family: Electrolize
      fonts:
        - asset: assets/fonts/Electrolize-Regular.ttf
